package models;

public class Admin extends User {
    public Admin(String name, String address, String phoneNumber, String username, String password) {
        super(name, address, phoneNumber, username, password);
    }
}
